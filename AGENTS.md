# Repository collaboration instructions

## Git commits

- After completing and verifying repository modifications requested by the user, automatically create a Git commit before the final response.
- Stage and commit only files related to the current request; preserve unrelated user changes.
- Do not create a commit for read-only investigation or explanation tasks.
- If verification or the commit fails, leave the changes uncommitted and report the blocker clearly.
